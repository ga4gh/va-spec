{
   "$schema": "http://json-schema.org/draft/2020-12/schema",
   "title": "CVC-MetaKB-Annotation-Definitions",
   "type": "object",
   "$defs": {
      "InformationEntity": {
         "description": "InformationEntities are abstract (non-physical) entities that are about something (i.e. they carry  information about things in the real world)."
      },
      "Contribution": {
         "description": "The sum of all actions taken by a single agent in contributing to the creation, modification,  assessment, or deprecation of a particular entity (e.g. a Statement, EvidenceLine, DataItem,  Publication, etc.)",
         "properties": {
            "id": {
               "type": "string",
               "description": "The 'logical' identifier of the entity in the system of record, e.g. a UUID. This 'id' is  unique within a given system. The identified entity may have a different 'id' in a different  system, or may refer to an 'id' for the shared concept in another system (e.g. a CURIE)."
            },
            "type": {
               "type": "string",
               "const": "Contribution",
               "default": "Contribution",
               "description": "MUST be \"Contribution\"."
            },
            "label": {
               "type": "string"
            },
            "extensions": {
               "type": "array",
               "items": {
                  "$ref": "core.json#/$defs/Extension"
               }
            },
            "agent": {
               "$ref": "#/$defs/Agent"
            },
            "date": {
               "type": "string",
               "format": "date"
            },
            "role": {
               "type": "string"
            }
         },
         "required": [
            "type"
         ],
         "additionalProperties": false
      },
      "Agent": {
         "description": "An autonomous actor (person, organization, or computational agent) that bears some form of responsibility for an activity taking place, for the existence of an entity, or for  another agent\u2019s activity.",
         "properties": {
            "id": {
               "type": "string",
               "description": "The 'logical' identifier of the entity in the system of record, e.g. a UUID. This 'id' is  unique within a given system. The identified entity may have a different 'id' in a different  system, or may refer to an 'id' for the shared concept in another system (e.g. a CURIE)."
            },
            "type": {
               "type": "string",
               "const": "Agent",
               "default": "Agent",
               "description": "MUST be \"Agent\"."
            },
            "label": {
               "type": "string"
            },
            "extensions": {
               "type": "array",
               "items": {
                  "$ref": "core.json#/$defs/Extension"
               }
            },
            "name": {
               "type": "string"
            }
         },
         "required": [
            "type"
         ],
         "additionalProperties": false
      },
      "Method": {
         "description": "A set of instructions that specify how to achieve some objective (e.g. experimental protocols,  curation guidelines, rule sets, etc.)",
         "properties": {
            "id": {
               "type": "string",
               "description": "The 'logical' identifier of the entity in the system of record, e.g. a UUID. This 'id' is  unique within a given system. The identified entity may have a different 'id' in a different  system, or may refer to an 'id' for the shared concept in another system (e.g. a CURIE)."
            },
            "type": {
               "type": "string",
               "const": "Method",
               "default": "Method",
               "description": "MUST be \"Method\"."
            },
            "label": {
               "type": "string"
            },
            "extensions": {
               "type": "array",
               "items": {
                  "$ref": "core.json#/$defs/Extension"
               }
            },
            "is_reported_in": {
               "$ref": "core.json#/$defs/CURIE"
            },
            "method_type": {
               "type": "string",
               "description": "A more specific type of entity the method represents (e.g. Variant Interpretation Guideline,  Experimental Protocol)"
            }
         },
         "required": [
            "type"
         ],
         "additionalProperties": false
      },
      "Statement": {
         "description": "A Statement (aka \u2018Assertion\u2019) represents a claim of purported truth as made by a particular agent,  on a particular occasion."
      },
      "VariationStatement": {
         "description": "A Statement describing the impact of a variation."
      },
      "VariationConditionStatement": {
         "description": "A Statement describing the impact of a variation on a condition."
      },
      "VariationGermlineConditionStatement": {
         "description": "A Statement describing the role of a variation in causing or protecting against a germline Condition.",
         "properties": {
            "id": {
               "type": "string",
               "description": "The 'logical' identifier of the entity in the system of record, e.g. a UUID. This 'id' is  unique within a given system. The identified entity may have a different 'id' in a different  system, or may refer to an 'id' for the shared concept in another system (e.g. a CURIE)."
            },
            "type": {
               "type": "string",
               "const": "VariationGermlineConditionStatement",
               "default": "VariationGermlineConditionStatement",
               "description": "MUST be \"VariationPathogenicityStatement\"."
            },
            "label": {
               "type": "string"
            },
            "extensions": {
               "type": "array",
               "items": {
                  "$ref": "core.json#/$defs/Extension"
               }
            },
            "description": {
               "type": "string",
               "description": "A free-text description of the InformationEntity."
            },
            "strength": {
               "description": "A coded term describing the strength of support that the information the statement represents is true.",
               "$ref": "core.json#/$defs/Coding"
            },
            "confidence_score": {
               "$ref": "#/$defs/DataItem",
               "description": "A quantitative score reflecting the degree of confidence that the information  the information entity represents is true."
            },
            "method": {
               "$ref": "#/$defs/Method",
               "description": "A Method that describes all or part of the process through which the information was generated."
            },
            "contributions": {
               "type": "array",
               "items": {
                  "$ref": "#/$defs/Contribution"
               }
            },
            "is_reported_in": {
               "type": "array",
               "items": {
                  "$ref": "#/$defs/Document"
               },
               "description": "A document in which the information content is expressed."
            },
            "record_metadata": {
               "$ref": "core.json#/$defs/RecordMetadata"
            },
            "direction": {
               "type": "string",
               "enum": [
                  "supports",
                  "uncertain",
                  "refutes"
               ],
               "description": "The direction of this statement with respect to the target proposition."
            },
            "subject_descriptor": {
               "description": "A descriptor characterizing the variation impacting the condition.",
               "$ref": "vod.json#/$defs/CategoricalVariationDescriptor"
            },
            "variation_origin": {
               "type": "string",
               "enum": [
                  "germline",
                  "somatic"
               ],
               "description": "A representation of whether the subject variation is inherited (germline) or acquired (somatic)."
            },
            "object_descriptor": {
               "description": "A descriptor characterizing the condition impacted by the variation.",
               "$ref": "vod.json#/$defs/ConditionDescriptor"
            },
            "classification": {
               "description": "The conclusion drawn from the statement proposition, direction, strength, and/or  confidence score.",
               "$ref": "core.json#/$defs/Coding"
            },
            "target_proposition": {
               "$ref": "#/$defs/VariationGermlineConditionProposition",
               "description": "The Proposition about which the Statement is made."
            }
         },
         "required": [
            "type"
         ],
         "additionalProperties": false
      },
      "Proposition": {
         "description": "An abstract ValueEntity representing the shareable meaning that can be  put forth as true or false by a Statement.",
         "required": [
            "subject",
            "predicate",
            "object"
         ]
      },
      "VariationProposition": {
         "description": "A proposition describing the role of a variation subject."
      },
      "VariationGermlineConditionProposition": {
         "description": "A proposition describing the role of a variation in causing or preventing a germline disease condition.",
         "properties": {
            "id": {
               "$ref": "core.json#/$defs/CURIE",
               "description": "The 'logical' identifier of the entity in the system of record, and MUST be represented as a CURIE. This 'id' is unique within a given system, but may also refer to an 'id' for the shared concept in  another system (represented by namespace, accordingly)."
            },
            "type": {
               "type": "string",
               "const": "VariationGermlineConditionProposition",
               "default": "VariationGermlineConditionProposition",
               "description": "MUST be \"VariationGermlineConditionProposition\""
            },
            "subject": {
               "description": "The `Variation` about which the Proposition is made.",
               "$ref": "catvars.json#/$defs/CategoricalVariation"
            },
            "predicate": {
               "type": "string",
               "description": "The relationship asserted to hold between the variation (subject) and  the condition (object) of the Proposition.",
               "enum": [
                  "causes_mendelian_condition",
                  "increases_risk_for_condition",
                  "decreases_risk_for_condition"
               ]
            },
            "object": {
               "description": "A ValueEntity that is related to the subject of a Proposition via its predicate.",
               "$ref": "core.json#/$defs/Condition"
            }
         },
         "required": [
            "type"
         ],
         "additionalProperties": false
      },
      "VariationNeoplasmProposition": {
         "description": "A proposition regarding the effect of a variation within a neoplasm."
      },
      "VariationNeoplasmTherapeuticResponseProposition": {
         "description": "A proposition describing the role of a variation in modulating the response of a neoplasm to one or more therapeutics."
      }
   }
}